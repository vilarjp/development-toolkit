---
description: Execute the implementation plan using TDD with parallel subagents
---

Execute the implementation plan using TDD with parallel subagents. Reads `02-plan.md` (as amended by `03-revision.md`), dispatches subagents per the Execution Waves table, enforces RED→GREEN→REFACTOR for every change. All tests must pass before this phase completes.

Usage: /execute [path to plan file, defaults to docs/spec/02-plan.md]
