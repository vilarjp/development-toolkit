---
description: Load and display the current project context
---

Load and display the current project context. Scans CLAUDE.md, project structure, config files, existing conventions, and active specs. Useful for understanding what the toolkit sees before starting a pipeline run.

Usage: /context
