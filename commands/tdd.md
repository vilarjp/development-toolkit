---
description: Enter TDD mode with RED-GREEN-REFACTOR enforcement
---

Enter TDD mode for the current task. Enforces RED→GREEN→REFACTOR cycle. Write a failing test first, then the minimum code to pass, then refactor. For bug fixes, uses the Prove-It pattern: reproduce with a failing test, then fix.

Usage: /tdd <description of what to implement or fix>
